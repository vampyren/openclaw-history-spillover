# Manual checklist

- Trigger a large tool output in chat history.
- Confirm oversized entry becomes a spillover stub, not a blind placeholder.
- Confirm a `history-oversize/*.json` file is created beside the session transcript.
- Confirm chat history still loads.
- Confirm restart preserves behavior.
